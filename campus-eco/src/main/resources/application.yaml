spring:
  application:
    name: CampusEcoResponsable

  datasource:
    url: jdbc:postgresql://localhost:5432/campus_eco?currentSchema=ecologie
    username: postgres
    password: 342742868
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: DEBUG